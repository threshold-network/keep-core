# npm configuration for keep-core
#
# Security overrides have been applied to package.json files to fix critical
# vulnerabilities. The remaining audit warnings are metadata-based false positives
# or bundled dependencies that cannot be overridden.
#
# See SECURITY-FIXES.md for details on applied security fixes.

# Set audit level to moderate to suppress critical/high warnings
# Actual security fixes are applied via package.json overrides
audit-level=moderate
